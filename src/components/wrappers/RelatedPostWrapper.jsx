import React from "react";

const RelatedPostWrapper = ({ children }) => {
  return (
    <section>
      <h2>Related Posts</h2>
      {children}
    </section>
  );
};

export default RelatedPostWrapper;
